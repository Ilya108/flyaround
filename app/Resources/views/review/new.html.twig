{% extends 'base.html.twig' %}

{% block body %}
<h1> New Review Form</h1>
    <div>
        {# renders all fields #}
        {{ form_widget(form) }}

        {# renders all fields *and* the form start and end tags #}
        {{ form(form) }}

    </div>

{% endblock %}