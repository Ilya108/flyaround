{% extends 'base.html.twig' %}

{% block body %}
    <h1>Planemodele</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planeModele.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ planeModele.model }}</td>
            </tr>
            <tr>
                <th>Manufacturer</th>
                <td>{{ planeModele.manufacturer }}</td>
            </tr>
            <tr>
                <th>Cruisespeed</th>
                <td>{{ planeModele.cruiseSpeed }}</td>
            </tr>
            <tr>
                <th>Planenbseats</th>
                <td>{{ planeModele.planeNbSeats }}</td>
            </tr>
            <tr>
                <th>Isavailable</th>
                <td>{% if planeModele.isAvailable %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('planemodele_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('planemodele_edit', { 'id': planeModele.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
